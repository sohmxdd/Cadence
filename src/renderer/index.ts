/**
 * Cadence — Main renderer process
 * This is the primary renderer window. In v1 it's mostly informational —
 * the real UI is the overlay pill (separate window, Phase 6).
 */

import './index.css';

console.log('Cadence renderer loaded');
